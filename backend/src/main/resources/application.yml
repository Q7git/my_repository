server:
  port: 8080

spring:
  application:
    name: demo-app

  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss
    
  datasource:
    url: jdbc:mysql://localhost:3306/demo?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: "@School969439"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
logging:
  level:
    root: info